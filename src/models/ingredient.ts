/* la création de ce model permettra d'avoir publiquement les données de notre liste saisie par le user */
export class Ingredient{
    constructor(public name: string,public amount: number){

    }
}